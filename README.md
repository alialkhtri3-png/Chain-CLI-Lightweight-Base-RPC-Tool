# Chain-CLI-Lightweight-Base-RPC-Tool
Chain CLI is a lightweight command-line tool that allows developers to query Base (and Ethereum) on-chain data directly via JSON-RPC, without API keys or heavy Web3 libraries.
